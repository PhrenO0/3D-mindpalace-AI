# 방 스캐너 v2 — Azure 검출 부착 & 레이캐스트 정밀화 (작업 설명서)

작성 기준: 2026-06-04
이전 설명서: `AI_ROOM_SCANNER_GUIDE.md` (전체 원리·암묵지)
이번 변경 파일: `server.mjs`, `personal-room-scanner-3d.html`, `.env(.example)`

---

## 0. 이번에 한 일 (한눈에)

1. **객체검출을 Azure AI Vision으로** 보낼 수 있게 함 (없으면 브라우저로 폴백).
2. **레이캐스트를 정밀화** — 단일 광선 추측 → "앞쪽 표면 군집 + 다시점 평균(삼각측량)".
3. Azure 키를 담을 **env 칸**과 키를 안전히 중계하는 **백엔드 프록시** 추가.

> 핵심 발상: **인식(무엇이 어디 보이나)은 Azure 클라우드**가, **3D 좌표(어디에 있나)는
> 클라이언트 레이캐스트**가 맡는 **하이브리드**. 각자 잘하는 걸 시킨다.

---

## 1. 왜 하이브리드인가 (암묵지)

지금까지는 "100% 브라우저(OWL-ViT)"였다. Azure를 붙이면 **검출만 클라우드 콜**이 된다.
이게 제약을 깨는 게 아니다:
- 배제 대상은 **Claude/Anthropic**이지 클라우드 자체가 아니다. **Azure는 우리 인프라**라
  "VM/서버에서 자립"에 부합한다.
- **정확한 인식**은 거대 데이터로 학습된 Azure 비전이 압도적으로 잘한다.
  반대로 **정확한 3D 좌표**는 GLB 형상이 손에 있는 **클라이언트 레이캐스트**가 정확하다.
  → 둘을 나누는 게 정답.

```
GLB(클라이언트) → 시점 렌더 → 이미지
   → POST /api/detect (백엔드 프록시) → Azure AI Vision → 박스+라벨
   → 박스로 레이캐스트(클라이언트) → 정확한 3D 좌표 → 핫스팟
```

**암묵지 ①: 왜 "백엔드 프록시"인가.**
Azure 키를 브라우저 JS에 두면 누구나 훔쳐 쓴다. 그래서 서버(`/api/detect`)가 이미지를 받아
Azure로 중계하고, **키는 서버 환경변수에만** 둔다. (VWorld 키를 다룬 철학과 같음.)

**암묵지 ②: 왜 "폴백"인가.**
키가 없거나 `file://`로 열면 Azure를 못 쓴다. 그때 죽지 않고 **브라우저 OWL-ViT로
자동 전환**해 계속 동작한다. → 키 없이도 데모 가능, 키 넣으면 즉시 고품질 전환.
판정은 `GET /api/vision-config`(`{azure:true/false}`) 한 번으로.

---

## 2. Azure를 켜는 법 (env)

`.env` 에 두 줄을 채우면 끝(비우면 자동 폴백):
```bash
AZURE_VISION_ENDPOINT=https://<리소스이름>.cognitiveservices.azure.com/
AZURE_VISION_KEY=<키1>
```
- Azure Portal → **Computer Vision**(또는 Azure AI Services) 리소스 생성 → "키 및 엔드포인트"에서 복사.
- 무료 티어(F0) 가능.
- 배포 시엔 App Service "구성 → 애플리케이션 설정"에 동일하게 넣는다.
- 값을 넣고 **서버 재시작** → 스캐너 상태표시줄에 "검출기: **Azure AI Vision**"으로 바뀐다.

쓰는 API: **Azure AI Vision 4.0 Image Analysis, `features=objects`** — 바운딩 박스 + 태그 반환.
서버가 픽셀 박스를 0~1 비율로 정규화해 클라이언트에 넘긴다. 라벨은 검출기마다 표기가 달라
(`couch`↔`sofa`) **공통 단어로 통일**(`LABEL_ALIAS`)한 뒤 개념에 매핑한다.

---

## 2.5 시점 최적화 — 2단계 타깃 스캔 (콜 수 고정, 광선↑)

삼각측량은 사물당 **2시점↑**이 필요한데 무료티어는 분당 20콜뿐. 그래서 맹목적 격자 대신
**같은 콜 수를 똑똑하게** 쓴다.
- **1차(개략, ~60%)**: 눈높이 격자×바깥보기로 "사물이 대략 어디" 파악(중앙 편향 방지).
- **2차(정밀, 나머지)**: 광선이 적은 사물부터, 그 사물을 **서로 벌어진 각도(base±0.9rad)에서 조준 촬영**
  → 잘 벌어진 baseline의 광선 2~3개 확보 → 삼각측량 가능.
- **콜 절약 필터**: 개념에 매핑되는 가구(`USEFUL_LABELS`)만 검출 채택 → 변기·"seating"에 콜 낭비 방지.

실측: 의자가 **7광선**까지 모여 정확히 삼각측량. 단, **다중 방 아파트**에선 같은 라벨이 여러 방에
잡혀 **데이터 연관이 흔들림**(라벨↔위치 어긋남) — 단일 방/실사진에서 더 잘 된다.

---

## 3. 레이캐스트 정밀화 (네가 지적한 약점 보완)

### 단점 1 — "박스 중심 한 점"이 사물 중심이 아님
박스 중심 광선이 사물 **틈**으로 빠져 **뒤 벽**을 맞을 수 있다.
**보완:** 박스 안쪽 3×3=9점을 쏴 여러 표면 후보를 모은다.

### 단점 2 — 여러 점을 쏴도 일부는 배경/뒤벽을 맞아 흩어짐
**암묵지 ③ (중요):** 검출된 사물은 박스 안에서 **카메라에 가장 가까운(앞쪽) 표면**이다.
그래서 9점 중 **가장 가까운 깊이**를 기준으로, 그와 비슷한 깊이(0.4m)인 점들만 모아
평균낸다. 멀리 흩어진(뒤벽) 점은 버린다.
> ⚠️ 처음엔 "가장 빽빽한 군집"을 골랐는데, 먼 사물을 향한 광선 다수가 **가까운 벽**을
> 맞아 그 벽이 빽빽해져 마커를 벽으로 끌어당겼다. → "가장 **가까운** 깊이층"으로 바꿔 해결.

### 단점 3 — 단일 광선은 추측, 위치가 흔들림
**암묵지 ④ (삼각측량):** 같은 사물을 **여러 각도에서** 본 점들을 모아 **평균**낸다.
여러 시점 광선이 만나는 곳이라 단일 광선보다 위치가 훨씬 안정적이다.
> ⚠️ 누적 평균으로 매칭 기준점을 움직였더니, 평균이 떠다니며 흩어진 점을 빨아들여
> 중앙으로 뭉쳤다. → 매칭 기준은 **첫 점(고정 앵커)**, 평균은 최종 좌표 계산에만 사용.

---

## 4. 이번에 또 배운 함정 (암묵지)

**렌더를 "사진처럼" 바꾸면 검출이 좋아질 것 같지만, 검출기에 따라 반대다.**
캡처 이미지에 톤매핑(ACES)을 넣었더니, **브라우저 OWL-ViT 폴백의 검출이 무너져**
핫스팟이 중앙으로 붕괴했다. → 되돌렸다.
교훈: **렌더 튜닝은 "실제로 쓰는 검출기"에 대해 검증해야** 한다. 톤매핑은 Azure(실사진 학습)
검증 후 다시 도입 예정.

---

## 5. 지금 상태 (검증 결과, 솔직)

| 항목 | 상태 |
|---|---|
| `/api/vision-config`, `/api/detect` 프록시 | ✅ 동작(키 없으면 `azure:false`→폴백) |
| 검출기 자동 전환(Azure↔브라우저) + 상태표시 | ✅ 동작 |
| 레이캐스트 v2(앞쪽 깊이층 + 다시점 평균) | ✅ 적용, 깨짐 없음 |
| 기존 테스트 8개 | ✅ 통과 |
| **브라우저 폴백 검출 정확도** | ⚠️ **여전히 약함** — 합성 GLB 렌더에서 OWL-ViT 신뢰도 14~35%, 일부 사물 누락·위치 흔들림 |
| **Azure 경로 실측** | ✅ **검증 완료** — 검출 **압도적 우수**(의자 84%, 식탁 73%, 싱크대 69%, TV 59%, 소파 56%). 폴백 OWL-ViT(14~35%)와 차원이 다름 |
| **3D 위치(occlusion)** | ✅ **해결(삼각측량)** — 표면을 맞히는 대신 **여러 시점 광선의 수렴점**을 계산. 광선이 중간 벽을 맞아도 교차점은 진짜 사물 위치. 실측: 의자가 6개 광선으로 정확히 수렴. |
| **남은 난제 — Azure 라벨 혼동** | ⚠️ 합성 렌더에서 **침대를 "dining table"로 오인식** 같은 라벨 오류가 남음. 삼각측량은 그 사물(침대)에 정확히 박지만 개념 매핑이 틀어짐. → 실사진/스캔이면↓, Custom Vision 학습으로 해결 가능. |

### occlusion 해결 방법 (삼각측량) — 암묵지
- **문제**: 문틈 너머로 검출된 가구는, 박스 중심 광선이 **중간 벽에 먼저 맞아** 마커가 벽에 박힌다.
- **틀린 접근**: "가장 가까운/빽빽한 표면"을 고르기 → 가까운 벽이 이김.
- **올바른 접근(구현)**: 표면을 맞히지 말고 **광선(원점+방향)만 저장**. 같은 사물을 여러 시점에서
  본 광선들은 **그 사물에서 수렴**한다. 광선 집합의 "가장 가까운 공통점"을 최소제곱으로 풀면
  (= 삼각측량) 가림과 무관하게 진짜 위치가 나온다.
- **데이터 연관(핵심)**: 어떤 광선들이 "같은 사물"인지 묶을 때, 표면 히트(가림되면 벽)가 아니라
  **광선끼리 최단접근(<0.85m, 둘 다 앞쪽)**으로 판정 → 가림이 있어도 같은 사물끼리 묶인다.
- **품질 우선**: 같은 라벨이 1-ray(단일시점·가림위험)와 2-ray(삼각측량)로 갈리면 **광선 많은 쪽**을 쓴다.
- **한계**: 사물당 광선이 2개 미만이면 삼각측량 불가(단일시점 폴백). 광선을 늘리려면 샷↑가 필요한데
  Azure 무료티어 분당 20콜 제한과 상충 → 유료티어/샷 최적화가 다음 과제.

### 실측에서 추가로 잡은 것
- **`/api/detect` 500 버그**: 전역 `express.json` 256KB 한도가 큰 PNG를 먼저 막음 → 20MB로 상향 해결.
- **Azure 무료(F0) 분당 20콜 제한**: 스캔 샷을 16개로 캡.
- **시점 전략(실측 결론)**: 위에서 수직으로 내려다보면 가구가 납작해 인식↓. **눈높이 각도가 최고**.
  다중 방은 **눈높이 격자×바깥보기**가 커버리지 최고, 단일 방은 **중앙 오비트 링**으로 보강(자동 전환).
- **테스트 임의 GLB 로드**: 스캐너에 `?glb=경로` 파라미터 추가.

> 결론: **v2의 구조·정밀화는 들어갔고**, 검출 품질의 실질 개선은 **Azure 키를 넣어
> 실측**해야 확인된다. 폴백(OWL-ViT)은 합성 렌더에서 본질적으로 노이즈가 커서,
> 이걸 더 튜닝하기보다 Azure로 검증하는 게 맞다.

---

## 6. 다음 단계

1. **Azure 키 넣고 실측** → 검출 품질·핫스팟 매칭 재확인(가장 먼저).
2. **스마트 시점 캡처** → GLB 서브메쉬 bbox로 카메라를 가구에 조준해 Azure 콜 수↓(비용·속도).
3. **세그멘테이션** → 박스 대신 사물 마스크로 배경 픽셀 제거(레이캐스트 정밀↑).
4. **핀 드래그 보정 UI** → 사용자가 어긋난 마커를 직접 수정(불안정성 실용적 보완).
5. (Azure 검증 후) **톤매핑·고해상도 캡처** 재도입.

---

## 7. 한 줄 정리

> 인식은 Azure(클라우드), 좌표는 레이캐스트(클라이언트)로 나누고,
> 레이캐스트는 "앞쪽 깊이층 + 다시점 평균"으로 흔들림을 줄였다.
> 다만 **품질의 진짜 도약은 Azure 키를 넣어 실측하는 순간** 온다 —
> 지금 폴백이 약한 건 합성 렌더 위 작은 모델의 한계이지, 구조의 한계가 아니다.
