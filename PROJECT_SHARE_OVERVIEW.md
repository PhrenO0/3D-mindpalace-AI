# 광화문 기억궁전 + AI 방 스캐너 — 전체 작업·암묵지 공유 문서

작성: 2026-06-04 · 프로젝트: `memory-palace-vworld` (3차 프로젝트)
상세 설명서: `AI_ROOM_SCANNER_GUIDE.md`(원리), `AI_SCANNER_V2_AZURE_GUIDE.md`(Azure·occlusion)
시각 자료: `TEAM_AI_SCANNER_SHARE.html`(브라우저로 열면 결과를 봄)

---

## 0. 이 프로젝트가 뭔가 (한 문단)

광화문 VWorld 3D 지도 위에 한옥 기억궁전을 세우고, 들어가면 방 안 사물에
조선 전기 학습 개념을 매핑해 "공간을 걸으며 외우는" 메모리 팰리스다.
여기에 **"사용자가 자기 방을 올리면 AI가 가구를 인식해 핫스팟을 자동으로 찍는"**
기능을 붙였다. 검출은 Azure, 3D 위치는 레이캐스트/삼각측량, 매핑은 규칙으로 한다.

---

## 1. 전체 작업 흐름 (시간순)

```
A. 광화문 VWorld 기억궁전 (기반)
   1) 서버 없이도 지도가 보이게 (키·GLB를 data:URI 임베드 → file:// 더블클릭 동작)
   2) 한옥 GLB가 안 보이던 문제 해결 (file:// XHR CORS 차단 → data:URI 우회)
   3) 한옥 위 GPS 마커 (작은 점→canvas 번호 핀 빌보드)
   4) 방 안 핫스팟을 실제 가구에 배치 + facing 버그(yaw 좌우반전) 수정
        │
        ▼
B. AI 방 스캐너 (자동화)
   5) 2D 사진 스캐너: 사진→객체검출+깊이추정→의사 3D 핫스팟
   6) 3D GLB 스캐너: GLB→여러 시점 렌더→검출→레이캐스트→정확한 3D 핫스팟
   7) 다지점 격자 스캔(중앙 한 점이면 칸막이에 가려 다른 방을 못 봄)
        │
        ▼
C. v2 — Azure & occlusion (고도화)
   8) 검출을 Azure AI Vision으로 (브라우저 OWL-ViT는 폴백)
   9) occlusion 해결: 표면 히트 대신 "다시점 광선 삼각측량"으로 위치 계산
```

---

## 2. 관통하는 핵심 원리 (암묵지)

### ① 핫스팟 = "3D 좌표 하나"
마커를 그리고 개념을 붙이는 건 쉽다. 진짜 어려운 건 **"화면에 보이는 저 소파가
3D 공간 어디에 있나"**를 알아내는 것. 모든 작업은 이 좌표를 구하는 문제였다.

### ② 그 좌표를 구하는 다리 = 레이캐스트
화면(2D)엔 깊이가 없다. 화면의 한 점에서 광선을 쏴 처음 부딪힌 표면의 3D 좌표를 읽는다.
- **손으로 할 땐** 내가 눈으로 그 점을 찍었고,
- **자동화에선** AI 검출 박스가 그 점을 준다.
→ 즉 자동화는 "내가 손으로 클릭하던 걸 AI가 대신"하는 것일 뿐, 발상은 같다.

### ③ "서버 없이"의 비밀 = data:URI
파일을 `file://`로 열면 브라우저가 GLB를 XHR로 못 불러온다(CORS). `data:` 스킴은
허용되므로, GLB를 base64로 코드에 박으면 네트워크 없이 로드된다. 키도 같은 식으로 분리.

### ④ AI는 클라우드, 좌표는 클라이언트 (하이브리드)
정확한 **인식**은 거대 학습된 Azure 비전이, 정확한 **3D 좌표**는 형상이 손에 있는
클라이언트 레이캐스트가 잘한다. 둘을 나눈다. (Claude/Anthropic 의존은 0, Azure는 자기 인프라)

### ⑤ occlusion = 표면 말고 "광선 교차점"
문틈 너머 가구를 보면 광선이 중간 벽에 먼저 맞는다. 표면을 고르면 벽이 이긴다.
대신 **여러 시점 광선이 수렴하는 점**을 삼각측량하면 가림과 무관하게 진짜 위치가 나온다.

---

## 3. 했던 작업 vs 안 했으면 안 됐던 버그들

| 증상 | 진짜 원인 | 해결 |
|---|---|---|
| 한옥/마커 안 보임(서버리스) | `file://` GLB가 CORS로 차단 | GLB를 data:URI 임베드 |
| 소파 누르면 침대가 보임 | 카메라 yaw 공식 x축 부호 반전 | `atan2(dx,-dz)`→`atan2(-dx,-dz)` |
| GPS 마커가 묻힘 | 작은 점이라 POI에 묻힘 | canvas 번호 핀 빌보드 |
| 핫스팟이 거실에만 뭉침 | 중앙 한 점에서만 봐 가려짐 | 다지점 격자/눈높이 시점 |
| `/api/detect` 500 | 전역 json 한도 256KB가 큰 PNG 차단 | 한도 20MB로 |
| 마커가 중간 벽에 박힘 | 가림된 표면을 좌표로 씀 | 다시점 광선 삼각측량 |

> 교훈: 안 되던 것 대부분은 "기능 미구현"이 아니라 **숨은 버그·구조적 오해**였다.
> AI를 붙이는 것보다 **AI가 잘 보도록 무대를 짜는 것**(어디서·어떻게 보게 할지,
> 좌표를 어떻게 정확히 잴지)이 결과 품질을 갈랐다.

---

## 4. 지금 상태 (검증 결과, 솔직)

### 잘 되는 것 ✅
- 광화문 지도·한옥·GPS마커·방 핫스팟: **서버 없이 더블클릭으로 동작**(검증).
- AI 방 스캐너: GLB→자동 핫스팟 **end-to-end 동작**.
- **Azure 검출이 압도적**: 의자 84%, 식탁 73%, 싱크대 69%, TV 59%, 소파 56%
  (브라우저 폴백 OWL-ViT 14~35%와 차원이 다름).
- **occlusion 해결**: 의자가 6개 광선으로 정확히 수렴(삼각측량).
- 기존 자동 테스트 8개 통과.

### 아직 불안정 ⚠️ (개선 진행 중)
- **Azure 라벨 혼동**: 합성 GLB 렌더에서 침대를 "dining table"로 오인식 등
  (삼각측량은 그 사물에 정확히 박지만 개념 매핑이 틀어짐). → 실사진/Custom Vision으로↓
- **사물당 광선 부족**: 삼각측량엔 같은 사물을 2시점↑ 봐야 하는데, Azure 무료티어
  분당 20콜 제한 때문에 샷이 적다. → 유료티어/시점 최적화 필요.
- 닫힌 셸형 스캔(분위기 GLB)은 조망·인식이 약함.

---

## 5. 직접 보는 법

```bash
npm install
npm run dev      # http://127.0.0.1:8765
```
- `vworld_3d_map_live.html` — 광화문 기억궁전 지도(키 있으면 3D)
- `personal-room-scanner-3d.html` — 3D GLB 방 스캐너 (`?glb=경로`로 임의 방 테스트)
- `personal-room-scanner.html` — 2D 사진 스캐너
- `TEAM_AI_SCANNER_SHARE.html` — 결과 showcase(그림 포함)

**Azure를 켜려면**: `.env`에 `AZURE_VISION_ENDPOINT`, `AZURE_VISION_KEY` 채우고 서버 재시작.
비워두면 브라우저 OWL-ViT로 자동 폴백(키 없이도 동작).

---

## 6. 다음 단계

1. **검출 라벨 품질**: Custom Vision으로 가구 클래스 학습(침대↔식탁 혼동 해결).
2. **시점/샷 최적화**: GLB 서브메쉬 bbox로 카메라를 가구에 조준 → 적은 콜로 많은 시점.
3. **세그멘테이션**: 박스 대신 사물 마스크로 배경 픽셀 제거(광선 정밀↑).
4. **핀 드래그 보정 UI**: 사용자가 어긋난 마커를 직접 수정.
5. **3D 스캔 업로드(Polycam 등)**: 진짜 걸어다니는 개인화 방.

---

## 7. 한 줄 요약

> 광화문 기억궁전을 서버 없이 띄우고, "내 방을 올리면 AI가 가구를 인식해 핫스팟을
> 자동으로 찍는" 데까지 왔다. **검출은 Azure(우수), 위치는 다시점 삼각측량(가림 해결)**.
> 남은 건 라벨 품질과 시점 수 — 구조가 아니라 데이터·튜닝의 문제다.
